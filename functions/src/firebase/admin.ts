/* import admin from "firebase-admin";

import serviceAccount from "./config.js";

const app = admin.initializeApp({
  credential: admin.credential.cert(JSON.stringify(serviceAccount)),
});

export default app; */

/* import admin from "firebase-admin";
import serviceAccount from "C:\\Users\\Administrador\\Downloads\\tevelam-5c6b4-firebase-adminsdk-fbsvc-efea4ac616.json" assert { type: "json" };

admin.initializeApp({
  credential: admin.credential.cert(JSON.stringify(serviceAccount)),
});
 */
